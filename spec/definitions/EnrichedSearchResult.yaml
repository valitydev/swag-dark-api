type: object
allOf:
  - type: object
    required:
      - payment
    properties:
      payment:
        $ref: '#/definitions/PaymentSearchResult'
      refund:
        $ref: '#/definitions/RefundSearchResult'